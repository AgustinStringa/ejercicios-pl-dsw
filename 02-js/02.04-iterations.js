const datos = [1, 5, 345, 7623, 6]

/**
 * element --> cada element
 * index --> posicion del elemento en el array
 * array --> el array mismo al que se le aplica foreach
 */
datos.forEach((element, index, array) => {
    // console.log(element);
    // console.log(array);
    console.log(index);
})